<nav class="navbar navbar-expand-lg bg-light sticky-top bg-white border-bottom" style="border-color: #afafaf">
  <div class="container-fluid w-75 ">
    <%= link_to root_path, class: 'navbar-brand' do %>
      <%= image_tag 'logo.png', width: '40rem' %>
      <%= image_tag 'pp_logo.png', width: '100rem' %>
    <% end %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="d-flex">
      <div class="collapse navbar-collapse me-5" id="navbarSupportedContent">
        <form class="d-flex" role="search">
          <input class="form-control me-2 border-0" type="search" placeholder="Search" aria-label="Search" , style="background: #efefef">
        </form>
      </div>
      <div class="navbar-collapse gap-4 ms-5">
        <%= link_to root_path, class: 'text-dark' do %>
          <i class="fa-sharp fa-solid fa-house-flag fa-xl"></i>
        <% end %>
        <i class="fa-sharp fa-solid fa-envelope fa-xl"></i>
        <a type="button" class="text-dark" data-bs-toggle="modal" data-bs-target="#staticBackdrop">
          <i class="fa-sharp fa-solid fa-circle-plus fa-xl"></i>
        </a>
        <i class="fa-sharp fa-solid fa-compass fa-xl"></i>
        <i class="fa-sharp fa-solid fa-heart fa-xl"></i>
        <div class="dropdown">
          <a class="" type="button" data-bs-toggle="dropdown" aria-expanded="false">
            <% if current_user && current_user.avatar.attached? %>
              <%= image_tag current_user.avatar, class: "img-thumbnail rounded-circle", style: "width: 2rem" %>
            <% else %>
              <%= image_tag 'default_avatar.png', class: "img-thumbnail rounded-circle", style: "width: 2rem" %>
            <% end %>
          </a>
          <ul class="dropdown-menu">
            <%= button_to 'Sign Out', destroy_user_session_path, method: :delete, class: "dropdown-item" %>
            <li><a class="dropdown-item" href="#">Something else here</a></li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</nav>

<%= render 'posts/form', post: Post.new %>
